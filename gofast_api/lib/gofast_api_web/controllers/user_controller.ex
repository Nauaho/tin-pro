defmodule GofastApiWeb.UserController do
	  use GofastApiWeb, :controller

  	def index(conn, _params) do

  	end

		def create(conn, %{"posts" => post_params}) do

		end

		def show(con, %{"id" => id}) do

		end

end
